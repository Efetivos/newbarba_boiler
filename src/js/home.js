
import { gsap } from 'gsap'
class Home {
    constructor() {
        return
    }

    init(contentPage) {
    }

    onScroll(scrolled) {
        //console.log(scrolled)
    }
}
export const home = new Home()