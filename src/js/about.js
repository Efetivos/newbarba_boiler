
import { gsap } from 'gsap'
class About {
    constructor() {
        return
    }

    init(contentPage) {
    }

    onScroll(scrolled) {
        //console.log(scrolled)
    }
}
export const about = new About()